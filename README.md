# Packing Match

Browser tool that matches an FMS packing pendency export (CSV) against the PM for Production
Google Sheets and lists the batches whose carton is sent, foil is issued and no foil was returned.

Everything runs in the browser. The sheet IDs are not stored in this repository; they are passed
in the page address after `#sheets=`.
